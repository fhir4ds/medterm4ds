"""Per-domain mixins for LocalDuckDBEngine.

The engine is split into mixins to keep individual files focused and
testable. LocalDuckDBEngine composes them; users should never import mixins
directly.
"""
